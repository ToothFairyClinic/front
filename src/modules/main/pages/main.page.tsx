import { FC } from "react";

interface MainPageProps {}

export const MainPage: FC<MainPageProps> = ({}) => {
  return (
    <div className="w-screen h-screen bg-white dark:bg-black transition  delay-150 "></div>
  );
};
